import React from "react";
import styled from "styled-components";
import { Grid, Container, Header, Image } from "semantic-ui-react";

const Wrapper = styled.div``;

export const ClothSim = () => (
    <Wrapper>
        <Container>
            <Header as='h2'>Cloth Simulator</Header>
            <p>
                test
            </p>
        </Container>
    </Wrapper>
);